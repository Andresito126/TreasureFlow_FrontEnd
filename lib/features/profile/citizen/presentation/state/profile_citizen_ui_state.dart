library;

enum MyPostsStatus { idle, loading, success, error }

enum DeletePostStatus { idle, deleting, done, error }

enum EditCitizenProfileStatus { idle, loading, success, error }

enum SaveCitizenProfileStatus { idle, saving, saved, error }
